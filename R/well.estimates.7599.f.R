#' xxx
#' 
#' \code{well.estimates.7599.f} yyy
#' 
#' @export

well.estimates.7599.f = function(year) {
  if(year == 1975) well.estimates <- well.estimates.1975
  if(year == 1976) well.estimates <- well.estimates.1976
  if(year == 1977) well.estimates <- well.estimates.1977
  if(year == 1978) well.estimates <- well.estimates.1978
  if(year == 1979) well.estimates <- well.estimates.1979
  if(year == 1980) well.estimates <- well.estimates.1980
  if(year == 1981) well.estimates <- well.estimates.1981
  if(year == 1982) well.estimates <- well.estimates.1982
  if(year == 1983) well.estimates <- well.estimates.1983
  if(year == 1984) well.estimates <- well.estimates.1984
  if(year == 1985) well.estimates <- well.estimates.1985
  if(year == 1986) well.estimates <- well.estimates.1986
  if(year == 1987) well.estimates <- well.estimates.1987
  if(year == 1988) well.estimates <- well.estimates.1988
  if(year == 1989) well.estimates <- well.estimates.1989
  if(year == 1990) well.estimates <- well.estimates.1990
  if(year == 1991) well.estimates <- well.estimates.1991
  if(year == 1992) well.estimates <- well.estimates.1992
  if(year == 1993) well.estimates <- well.estimates.1993
  if(year == 1994) well.estimates <- well.estimates.1994
  if(year == 1995) well.estimates <- well.estimates.1995
  if(year == 1996) well.estimates <- well.estimates.1996
  if(year == 1997) well.estimates <- well.estimates.1997
  if(year == 1998) well.estimates <- well.estimates.1998
  if(year == 1999) well.estimates <- well.estimates.1999
  
  return(well.estimates)
}